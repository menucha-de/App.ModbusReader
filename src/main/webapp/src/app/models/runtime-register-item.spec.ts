import { RuntimeRegisterItem } from './runtime-register-item';

describe('RuntimeRegisterItem', () => {
  it('should create an instance', () => {
    expect(new RuntimeRegisterItem()).toBeTruthy();
  });
});
