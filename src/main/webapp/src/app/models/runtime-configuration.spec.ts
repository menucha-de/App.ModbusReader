import { RuntimeConfiguration } from './runtime-configuration';

describe('RuntimeConfiguration', () => {
  it('should create an instance', () => {
    expect(new RuntimeConfiguration()).toBeTruthy();
  });
});
